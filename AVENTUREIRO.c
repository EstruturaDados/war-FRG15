#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

typedef struct {
    char [30];
    cor [10];
    int tropas;
} territorio;

void ataque (territorio *t, int at, int def) {
  int dadocante = rand() % 6 + 1;
  int dadoensor = rand() % 6 + 1; 
printf("")
